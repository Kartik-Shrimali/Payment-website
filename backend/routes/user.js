const express = require('express');

const router = express.router();

router.post('/signup' , (res , req)=>{

})

router.post('/signin' , (res , req)=>{
    
})

router.put('/changepassword' , (res , req)=>{
    
})

module.exports = {
    router
}